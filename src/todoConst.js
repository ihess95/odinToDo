function todoConst(title, desc, dueDate, priority, notes) {
  this.cardTitle = title;
  this.cardDesc = desc;
  this.cardDueDate = dueDate;
  this.cardPriority = priority;
  this.cardNotes = notes;
}
export { todoConst };
